# Maria Yurko

A minimal single-page email capture. Static HTML/CSS/JS — no build step.

## Run locally

```bash
python3 -m http.server 8000
# open http://localhost:8000
```

## Deploy

Any static host works. Vercel / Netlify / GitHub Pages — just point at the repo root.
